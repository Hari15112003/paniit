import 'package:flutter/material.dart';

class InstructorCourseAddPage extends StatelessWidget {
  const InstructorCourseAddPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const SafeArea(
      child: Scaffold(
        body: Column(
          children: [],
        ),
      ),
    );
  }
}
